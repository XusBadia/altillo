import SwiftUI

/// STUB (phase 12): replaced by the clipboard utility work.
struct DesvanClipboardView: View {
    let model: NotchModel

    var body: some View {
        DesvanModuleNotice(symbol: "list.clipboard", title: "Clipboard")
    }
}
