import AltilloCore
import Foundation

/// Fake content for the design review (usage, agents, now playing…). STUB: filled by the visual design work.
struct DemoContent {
    static let sample = DemoContent()

    /// Sample shelf items shown by the design scenarios that need a non-empty shelf.
    var shelfItems: [ShelfItem] = []
}
