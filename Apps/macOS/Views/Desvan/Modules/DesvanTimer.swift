import SwiftUI

/// STUB (phase 12): replaced by the timer utility work.
struct DesvanTimerView: View {
    let model: NotchModel

    var body: some View {
        DesvanModuleNotice(symbol: "timer", title: "Timer")
    }
}
