// altillo-hook: forwards coding-agent hook events to Altillo. Implemented in phase 4 (PLAN.md §5.3).
// Until then it must stay a no-op that never blocks the agent: exit 0 with no output.
import Foundation

exit(0)
