import SwiftUI

/// STUB (phase 12): replaced by the note utility work.
struct DesvanNoteView: View {
    let model: NotchModel

    var body: some View {
        DesvanModuleNotice(symbol: "note.text", title: "Note")
    }
}
